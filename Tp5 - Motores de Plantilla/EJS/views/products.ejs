<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Vista de Productos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
</head>

<body>
    <div class="container mt-3">
        <!-- incrustar estilos -->

            <div class="jumbotron">
                <h1>Vista de Productos</h1>
                <br>
                <!-- el valor hayProductos deberia llegar del servidor como un flag boolean para saber si el arreglo esta vacio o lleno -->
                <% if(getProducts){ %>
                    <div class="table-responsive">
                        <table class="table table-dark">
                            <tr>
                                <th>Nombre</th>
                                <th>Precio</th>
                                <th>Foto</th>
                            </tr>
                            <!-- implementar foreach para cargar productos -->
                            <% getProducts.forEach((item)=>{ %>
                            <div class="table-responsive">
                                    <table class="table table-dark">
                                        <tr>
                                            <th><%= item.title %></th>
                                            <th><%= item.price %></th>
                                            <th><img src=<%= item.thumbnail %> alt="thumbnail"></th>
                                        </tr>
                            <%});%>
                        </table>
                    </div>
                    <% } else{ %>
                        <h3 class="alert alert-warning"><%= error %></h3>
                        <% } %>

                            <br>
                            <button type="'button" class='btn btn-primary' onclick="location.href='/'">Cargar producto</button>

            </div>
    </div>


</body>

</html>